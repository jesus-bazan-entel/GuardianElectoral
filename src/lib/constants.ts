// Default branding (used before tenant is loaded)
export const DEFAULT_COLORS = {
  primary: "#1e40af",
  secondary: "#d97706",
};

export const PIN_LENGTH = 6;

export const POINTS = {
  PER_MESA: 10,
  PER_ACTA: 5,
  PER_FULL_TOP3: 2,
};
// CI/CD pipeline active
